package com.example.mmdp;

public class implicit_intent {


/*

    public void openWeb (String url)
    {
        Uri webpage = Uri.parse(url);
        Intent i = new Intent(Intent.ACTION_VIEW,webpage);
        if (i.resolveActivity(getPackageManager()) != null)
        {
            startactivity

        }


    }
    */
}
